import React from "react";
import CreateLoanInvoice from "./loan";

const LoanInvoice = ({ navigation, route }) => {
  return <CreateLoanInvoice navigation={navigation} route={route} />;
};

export default LoanInvoice;
